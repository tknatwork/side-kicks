{
  "name": "@dsb/guardrails",
  "version": "0.1.0",
  "private": true,
  "description": "Sandbox enforcement, path validation, file policy, audit logging, rollback, integrity verification, and tamper response",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "test": "vitest run",
    "test:unit": "vitest run",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}
