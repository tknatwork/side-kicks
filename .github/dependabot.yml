# Dependabot — automated dependency update PRs
# https://docs.github.com/en/code-security/dependabot/dependabot-version-updates/configuration-options-for-the-dependabot.yml-file
#
# Updates land as PRs targeting `main` with the `dependencies` label.
# Branch protection still requires owner approval before merge — Dependabot
# cannot self-merge unless explicitly granted, which is not configured here.
#
# Only projects with real dependency manifests are configured here. The former
# Design System Builder and nectar-design-toolkit projects were removed, so their
# ecosystems are gone. natural-scroll-switch is SwiftPM with ZERO dependencies, so
# it needs no package ecosystem; its GitHub Actions are SHA-pinned and are already
# covered by the `github-actions` entry at `/` below.

version: 2
updates:
  # ── npm / pnpm ─────────────────────────────────────────────────────────────

  # variables-styles-extractor — published Figma plugin
  - package-ecosystem: "npm"
    directory: "/variables-styles-extractor"
    schedule:
      interval: "weekly"
      day: "monday"
    open-pull-requests-limit: 5
    labels:
      - "dependencies"
      - "npm"
    commit-message:
      prefix: "chore(deps)"
      include: "scope"
    groups:
      # Group dev-only updates into a single PR to reduce noise
      dev-dependencies:
        dependency-type: "development"

  # ── GitHub Actions ─────────────────────────────────────────────────────────

  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "weekly"
      day: "monday"
    open-pull-requests-limit: 5
    labels:
      - "dependencies"
      - "github-actions"
    commit-message:
      prefix: "chore(actions)"

  - package-ecosystem: "github-actions"
    directory: "/variables-styles-extractor"
    schedule:
      interval: "weekly"
      day: "monday"
    open-pull-requests-limit: 5
    labels:
      - "dependencies"
      - "github-actions"
    commit-message:
      prefix: "chore(actions)"
